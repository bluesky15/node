'use strict';

function isNumber(arg) {
  return typeof arg === 'number';
}

function isBoolean(arg) {
  return typeof arg === 'boolean';
}

function isString(arg) {
  return typeof agr === 'string';
}

function isFunction(arg) {
  return typeof arg === 'function';
}